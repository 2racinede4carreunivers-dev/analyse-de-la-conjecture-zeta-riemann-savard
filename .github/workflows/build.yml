PS C:\univers_carre> notepad .github\workflows\build.yml
PS C:\univers_carre> type .github\workflows\build.yml
name: Build Isabelle + LaTeX (version courte active et neutralisÃ©e)

on:
  workflow_dispatch:   # Lancement manuel uniquement
  # push:
  #   branches:
  #     - main          # DÃ©commenter pour activer le dÃ©clenchement automatique

# ---------------------------------------------------------
# VERSION NEUTRALISÃ‰E (active par dÃ©faut)
# ---------------------------------------------------------
jobs:
  build-and-attest:
    if: false   # Neutralisation totale du workflow
    runs-on: ubuntu-latest
    steps:
      - name: Neutralisation
        run: echo "Workflow neutralisÃ©. Voir README pour l'activer."

# ---------------------------------------------------------
# VERSION ACTIVE (dÃ©sactivÃ©e par dÃ©faut)
# Pour activer : dÃ©commenter ce bloc et commenter le bloc neutralisÃ©
# ---------------------------------------------------------

# jobs:
#   build-and-attest:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#
#       - name: Install minimal TeX Live + pygments
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y \
#             texlive-latex-base \
#             texlive-latex-recommended \
#             texlive-latex-extra \
#             texlive-fonts-recommended \
#             python3-pygments
#
#       - name: Install Isabelle 2024
#         run: |
#           wget https://isabelle.in.tum.de/website-Isabelle2024/dist/Isabelle2024_linux.tar.gz
#           tar -xzf Isabelle2024_linux.tar.gz
#           mv Isabelle2024 isabelle
#           echo "$PWD/isabelle/bin" >> $GITHUB_PATH
#
#       - name: Isabelle build (certification HOL)
#         run: |
#           if [ -f ROOT ]; then
#             echo "ROOT dÃ©tectÃ© â€” compilation complÃ¨te"
#             isabelle build -D .
#           else
#             echo "Pas de ROOT â€” certification des .thy individuels"        
#             shopt -s nullglob
#             for thy in *.thy; do
#               echo "Certification de $thy"
#               isabelle build -b -d . "$thy" || true
#             done
#           fi
#
#       - name: Compile LaTeX (minted + sml + TOC)
#         run: |
#           shopt -s nullglob
#           for file in *.tex; do
#             echo "Compilation de $file"
#             pdflatex -shell-escape -interaction=nonstopmode "$file" || true  
#             pdflatex -shell-escape -interaction=nonstopmode "$file" || true  
#             pdflatex -shell-escape -interaction=nonstopmode "$file" || true  
#           done
#
#       - name: Upload PDFs
#         uses: actions/upload-artifact@v4
#         with:
#           name: pdfs
#           path: "*.pdf"
PS C:\univers_carre> 